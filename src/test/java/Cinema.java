import org.junit.Assert;
import org.junit.Test;

public class Cinema {
    public static void main(String args []){

        // Attempted Testing however even after applying junit dependency to pom @Test is not recognised

      /* @Test
        CalculationTest cal = new CalculationTest ();
        int answer = cal.add(8, 6);
        Assert.assertEquals("Calculation failed", 14, answer); */
    }
}
